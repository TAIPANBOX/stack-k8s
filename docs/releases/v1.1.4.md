# stack-k8s 1.1.4

The notifier at 0.2.3 (heraldyx `docs/releases/v0.2.3.md`, `45-heraldyx.yaml`,
opt-in): the mail for a provider that answered and refused says so, instead of
describing it as unreachable and free. With tokenfuse at 1.0.1 (1.1.3) that
refusal reaches the bus, so this is the other half of the same change. Nothing
else in the pinned set moves. **This pin moved without a cluster run, by
decision on 2026-09-14**; the image is the same program with one sentence
changed, held by its own test.

## The pinned set, 2026-09-14

As in 1.1.3, with `heraldyx:v0.2.3` in place of v0.2.2.

## Not in this release, said plainly

- No cluster run; `verify.sh`'s bus-line check (1.1.2) and the 1.0.1 gateway
  (1.1.3) are still unseen on a live cluster.
